import { Chip, styled } from "@mui/material";

const MainChip = styled(Chip)(() => ({
  padding: '30px 20px',
  borderRadius: 44,
  fontWeight: 500,
  fontSize: 20,
  color: "#ffffff",
}))

export default MainChip;